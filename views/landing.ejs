<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
    <title>Memory Mate - Welcome</title>
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #f6f8f9 0%, #e5ebee 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .landing-container {
            text-align: center;
            background-color: white;
            padding: 3rem 2rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            max-width: 500px;
            width: 100%;
        }
        .brain-logo {
            max-width: 250px;
            margin-bottom: 2rem;
            transition: transform 0.3s ease;
        }
        .brain-logo:hover {
            transform: scale(1.05);
        }
        .purp-btn {
            background-color: #6a5acd;
            color: white;
            border: none;
            padding: 12px 24px;
            margin: 10px;
            border-radius: 50px;
            text-transform: uppercase;
            font-weight: 600;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }
        .purp-btn:hover {
            background-color: #5a4acd;
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 4px 17px rgba(0, 0, 0, 0.2);
        }
        .welcome-text {
            color: #333;
            margin-bottom: 2rem;
        }
    </style>
</head>
<body>
    <div class="container landing-container">
        <%if(error && error.length>0){%>
            <div class="alert alert-danger" role="alert">
                  <%=error%>
            </div>
        <%}%>
        <%if(success && success.length>0){%>
            <div class="alert alert-success" role="alert">
                  <%=success%>
            </div>
        <%}%>
        
        <img src="img/MemoryMate.png" alt="Memory Mate Logo" class="brain-logo" height="200">
        <h2 class="welcome-text"><strong>Welcome to Memory Mate</strong></h2>

        <div>
            <a href="/register" class="purp-btn">Register</a>
            <a href="/login" class="purp-btn">Login</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>