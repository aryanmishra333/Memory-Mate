<!DOCTYPE html>
<html>
<head>
    <title>Virtual Companion</title>
    <link rel="stylesheet" href="/stylesheets/app.css">
</head>
<body>
    <div class="header">
        <a href="/home">
            <img src="/img/back.png" alt="" height="50">
        </a>
    </div>

    <div class="container">
        <h2>Virtual Companion</h2>
        <p>Talk to your AI companion for emotional support and memory prompts.</p>

        <form action="/ask-companion" method="POST">
            <input type="text" name="message" placeholder="Ask me anything..." required>
            <button type="submit">Send</button>
        </form>

        <% if (typeof response !== "undefined" && response.trim() !== "") { %>
            <div class="response">
                <h3>Companion's Response:</h3>
                <p><%= response %></p>
            </div>
        <% } %>
    </div>
</body>
</html>
